[project]
name = "flutter-demo-e2e"
version = "0.1.0"
description = "Selenium + pytest E2E for /ui/"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.0",
  "selenium>=4.24",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra"
